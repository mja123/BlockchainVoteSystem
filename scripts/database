CREATE SCHEMA vote_schema;

CREATE TABLE IF NOT EXISTS vote_schema.users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    vote_hash VARCHAR(255)
)
